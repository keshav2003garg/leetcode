import React from 'react';

interface AppProps {}

export const App: React.FC<AppProps> = ({}) => {
    return (
        <div>
            <h1 className='text-xl text-center'>App</h1>
        </div>
    );
};
